# The .pdb (38 MB) only makes crash loggers show function names. Publish it as a
# loose file on Nexus or a GitHub Release rather than versioning it in the repo.
symbols/

# The installable archive is regenerated from Data/ and Docs.
*.zip
