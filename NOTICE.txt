DtryKeyUtil
Original mod by D7ry (Dtry on Nexus Mods)
Upstream source: https://github.com/D7ry/DtryKeyUtil

This package is NOT the mod. It is a compatibility patch containing only the SKSE
plugin (DtryKeyUtil.dll), recompiled from the official source, with no changes to
the mod's design or features.

LICENSING OF THIS PATCH

This patch is distributed under the GNU General Public License version 3 or later
(see LICENSE), WITH the Modding Exception and GPL-3.0 Linking Exception granted
by CommonLibSSE-NG (see LICENSE-CommonLibSSE-NG-EXCEPTIONS.md), and the complete
corresponding source is published in the patch repository, under
source/complete-source/, as GPL-3 section 6 requires:
  https://github.com/Juan-MZ/DtryKeyUtil-1.7.99-Patch
Section 6(d) permits offering it from a designated place rather than bundling it
with the binary, so this archive carries the DLL, the licences and this notice.

Why: the compiled DtryKeyUtil.dll statically links CommonLibSSE-NG 6.7.0 (commit
3d8161461791), which is GPL-3.0-or-later. A plugin that statically links it forms
a combined work and must be distributed under GPL-3.0-or-later or a
GPL-compatible licence. CommonLibSSE-NG was MIT-licensed until August 2026; the
original mod predates that change, so D7ry never faced this.

  KNOWN GAP: unlike the other mods in this series, the upstream DtryKeyUtil
  repository carries NO licence file at all. GPL-3 distribution of the combined
  work assumes the mod's own portion is under a GPL-compatible licence, and an
  absent licence is not one. D7ry's Nexus permission grant (below) is broad -
  broader in spirit than MIT - but it is a permission to modify and release, not
  an explicit licence grant that covers redistributing their code under GPL-3.
  The clean fix is for D7ry to add an explicit licence (MIT would match the
  spirit of their permissions); that request is included in the courtesy message
  in nexus/03-message-to-author.md. Until then this is published in good faith on
  the strength of that grant, and will be withdrawn on request.

LEGAL BASIS FOR MODIFYING AND RELEASING
The mod page's own permission settings state:

  Modification permission:
    "You are allowed to modify my files and release bug fixes or improve on the
     features without permission from or credit to me."

That grant is what permits this rebuild. The mod page also states that the file may
not be uploaded to OTHER sites, which is respected: this patch is not mirrored
anywhere else.

CHANGES FROM THE ORIGINAL:
  - Rebuilt against CommonLibSSE-NG 6.7.0 to read Address Library format 5 and the
    Skyrim 1.7.99 structure layouts. CommonLib is now built from source, because the
    "commonlibsse-ng-flatrim" vcpkg port the mod used is far too old.
  - src/utils.h: logger::info() was being passed a runtime string as its format
    string. Newer fmt rejects this at compile time, and it was a latent bug: an INI
    value containing braces would have been parsed as a format specifier.
  - CMakeLists.txt / src/main.cpp: the plugin declaration is now written by hand with
    SKSE::PluginVersionData instead of the generated SKSEPluginInfo(). The generated
    form cannot set the Address Library v5 compatibility flag, because its
    StructCompatibility field occupies the same offset as versionIndependenceEx.
  - vcpkg.json: updated baseline, dropped the unused boost-stl-interfaces and
    rsm-binary-io dependencies and the fmt 8.0.1 pin; vcpkg-configuration.json
    removed (the colorglass registry is no longer used).

Third-party code: src/lib/SimpleIni.h is MIT-licensed, by Brodie Thiesfield.
Not affiliated with D7ry or Bethesda.
